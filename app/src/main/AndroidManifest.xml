<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="rafael.reserva">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".LoginActivity">
        </activity>
        <activity android:name=".RegisterChoice">
        </activity>
        <activity android:name=".UserRegister">
        </activity>
        <activity android:name=".RestaurantRegister">
        </activity>
        <activity android:name=".Main2ActivityUser">
        </activity>
        <activity android:name=".Main2ActivityRestaurant">
        </activity>
        <activity android:name=".FazerReserva">
        </activity>
    </application>

</manifest>